def solution(A):
    if not A:
        return 0
    max_price = 0
    min_price = A[0]
    for price in A:
        min_price = min(min_price, price)
        max_price = max(max_price, price - min_price)
    return max_price
