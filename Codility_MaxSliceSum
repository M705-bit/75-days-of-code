def solution(A):
    try:
        max_ending = A[0]
        max_sum = A[0]
    except TypeError:
        return A
    for a in range(1,len(A)):
        max_ending= max(A[a], max_ending+A[a])
        max_sum = max(max_sum, max_ending)
    return max_sum
